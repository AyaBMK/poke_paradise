{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="container pt-5 mt-5">
<a href="{{ path('app_category_crud_index') }}"><i class="fa-solid fa-arrow-left"></i> Revenir à la liste</a>
<h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('app_category_crud_edit', {'id': category.id}) }}" class="mb-2"><i class="fa-solid fa-pen-to-square"></i></a>

    <span>{{ include('category_crud/_delete_form.html.twig') }}</span>

</div>    
{% endblock %}
